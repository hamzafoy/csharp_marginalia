//Integers are declared using `int`
int a = 2;
//The type for `b` is int because `a` stores an integer and is added to 3 which is also an integer.
//There is no type conflict because the expression `a + 3` results in an integer.
int b = a + 3;

Console.WriteLine(b);

int monthlyWage = 1234;
int months = 12, bonus = 1000;
//You can declare more than one variable of the same type on the same line.